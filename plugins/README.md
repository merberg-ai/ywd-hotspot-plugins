# Plugins

Real YWD-Hotspot plugin projects belong under this directory, one directory per plugin ID.

Keep each plugin self-contained and compatible with the canonical YWD-Hotspot package builder. Executable service plugins require trusted Ed25519 signing before upload.

The first real plugin will be added here after its purpose, data inputs, configuration, and minimum required capabilities are defined.
